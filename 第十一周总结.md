# wncg实验室第十一周总结

1.怎样定义函数之后能自己控制输入的值(已经解决）(输入input)

2.input只能输入字符，不能输入数字（已经解决）输入int(intput())

3.水仙花数（已完成）

4.markdown（已完成，但具体不知道有什么作用）

5.python（已看完函数部分，但有很多知识感觉不能够具体运用到程序里面去）

例如：看完dict过后我想自己编一个输入人名输出对应成绩的程序，但用那一章所讲的知识还不能做到

不是特别清楚Visual Studio Code和其他编译器的区别，感觉廖雪峰网站上能编译的程序，我这上面不行。

递归函数那一部分基本看不懂

6.为什么单独输入数字的时候必须用int(intput())而使用a, b = input().split()不需要强调输入的值为整型呢

7.输入200个激活码未完成，百度出来的结果基本看不懂，很多命令完全没看到过

8.深度学习一二章已看完
